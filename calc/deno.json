{
  "imports": {
    "@std/assert": "jsr:@std/assert@^1"
  },
  "tasks": {
    "test": "deno test --allow-read",
    "repl": "deno run --allow-read --allow-env --allow-write --allow-sys repl.ts",
    "build": "cd grammar && nix-shell -p tree-sitter emscripten --run 'tree-sitter generate && tree-sitter build --wasm -o ../lib/parser.wasm'"
  }
}
